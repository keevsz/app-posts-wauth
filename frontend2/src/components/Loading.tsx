import { BoxLoading, Loader } from "@/styled-components/Loading.styled";

const Loading = () => {
  return (
    <BoxLoading>
      <Loader></Loader>
    </BoxLoading>
  );
};
export default Loading;
